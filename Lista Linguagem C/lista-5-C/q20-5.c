/* Questão 20. Implemente um algoritmo para calcular o resultado da série abaixo. Os valores de n e x deverão ser
digitados pelo usuário. O programa deve mostrar os elementos que compõem a série e a soma dos elementos. */
#include <stdio.h>
int main(){
    int n, x;
    
    printf("n: ");
    scanf("%d", &n);
	printf("x: ");
    scanf("%d", &x);
    
    
	
    return 0;
}
